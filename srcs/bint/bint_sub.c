#include "bint.h"

t_bint * bint_sub(t_bint * a, t_bint * b) {
		(void)a;
	(void)b;
	return (NULL);
}

t_bint * bint_sub_dst(t_bint ** dst, t_bint * a, t_bint * b) {
	(void)dst;
	(void)a;
	(void)b;
	return (NULL);
}

#include "bint.h"

t_bint * bint_mod(t_bint * a, t_bint * b) {
	(void)a;
	(void)b;
	return (NULL);
}

t_bint * bint_mod_dst(t_bint ** dst, t_bint * a, t_bint * b) {
	(void)dst;
	(void)a;
	(void)b;
	return (NULL);
}

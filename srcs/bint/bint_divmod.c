#include "bint.h"

/** big endian */
t_bint ** bint_divmod(t_bint * a, t_bint * b) {
	(void)a;
	(void)b;
	return (NULL);
}

t_bint ** bint_divmod_dst(t_bint ** *dst, t_bint * a, t_bint * b) {
	(void)dst;
	(void)a;
	(void)b;
	return (NULL);
}
